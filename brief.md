program will need to

be able to add api token

do search with parameters:
    minRating int
    maxRating int
    website bool
    location string
    limit to give back int
    reviewcount
    output format


returns:

    name
    rating
    website
    email
    location
    phone
    reviewcount

output in CSV or CLI