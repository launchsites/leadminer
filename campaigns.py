# campaign command
    # create campaign
    # select campaign
    # list campaigns
    # remove campaigns
    # list data of campaign

def campaign(command: str, option: str | None = None):

    if command == "create":
        # handle creating one with name option

    elif command == "select":
        # handle selectingi the one with name option

    elif command == "list":
        if option:
            #handle listing the data from campaign
        else:
            # list all the campaigns

    elif command == "remove":
        # remove the campaign